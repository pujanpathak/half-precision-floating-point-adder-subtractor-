module twosComplement #(parameter N = 5)(
	input [N-1:0] A,
	output [N-1:0] Z
);

logic [N-1:0] holder;
assign holder = ~A; //flipping the bits
assign holder = holder + 1; //adding 1 to the inverted bit

assign Z = holder;

endmodule  